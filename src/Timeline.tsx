import React from 'react';
import { Box, Typography } from '@mui/material';

const Timeline = () => {
  return (
    <Box sx={{ p: 2 }}>
      <Typography variant="h1">Timeline</Typography>
      {/* 他のユーザーの投稿を表示するコンポーネントやデザインをここに追加 */}
    </Box>
  );
};

export default Timeline;
